#ifndef COMMON_MINMAX_H_
#define COMMON_MINMAX_H_

#ifndef MIN
#define MIN( a, b ) ( ( a ) < ( b ) ? ( a ) : ( b ) )
#endif

#ifndef MAX
#define MAX( a, b ) ( ( a ) >= ( b ) ? ( a ) : ( b ) )
#endif

#endif /* COMMON_MINMAX_H_ */
